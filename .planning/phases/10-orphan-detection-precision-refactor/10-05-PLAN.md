---
phase: 10-orphan-detection-precision-refactor
plan: 05
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json
  - tests/DiskScout.Tests/CorpusAcceptanceTests.cs
  - src/DiskScout.App/Services/IAuditCsvWriter.cs
  - src/DiskScout.App/Services/AuditCsvWriter.cs
  - src/DiskScout.App/App.xaml.cs
  - tests/DiskScout.Tests/AuditCsvWriterTests.cs
  - tests/DiskScout.Tests/DiskScout.Tests.csproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/fixtures/programdata_corpus_365.json contains 365 corpus items with human verdicts (Aucun/Faible/Moyen/Eleve/Critique + RecommendedAction)"
    - "Acceptance test RemanentDetector_Should_Match_Manual_Audit reaches >= 95% concordance (within 1 RiskLevel step)"
    - "Acceptance test reaches 0% Critique-classified-by-human items recommended Supprimer or CorbeilleOk by the engine"
    - "Mode --audit CLI produces a CSV at %LocalAppData%\\DiskScout\\audits\\audit_YYYYMMDD_HHmmss.csv"
    - "CSV columns: Path, SizeBytes, ConfidenceScore, RiskLevel, TriggeredRules, ExonerationRules, RecommendedAction"
  artifacts:
    - path: "tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json"
      provides: "365-item corpus with human verdicts (the precision benchmark)"
      contains: "\"verdict\""
    - path: "tests/DiskScout.Tests/CorpusAcceptanceTests.cs"
      provides: "Concordance >=95% + zero-Critique-misclassification acceptance"
      contains: "RemanentDetector_Should_Match_Manual_Audit"
    - path: "src/DiskScout.App/Services/AuditCsvWriter.cs"
      provides: "CSV exporter for --audit mode"
      contains: "public sealed class AuditCsvWriter"
  key_links:
    - from: "src/DiskScout.App/App.xaml.cs"
      to: "AuditCsvWriter + AppDataOrphanPipeline"
      via: "OnStartup --audit branch"
      pattern: "--audit"
    - from: "tests/DiskScout.Tests/CorpusAcceptanceTests.cs"
      to: "AppDataOrphanPipeline (10-04)"
      via: "constructed pipeline + per-item EvaluateAsync"
      pattern: "EvaluateAsync"
---

<objective>
Validate Phase 10's precision claim against the user's 365-item corpus from `C:\ProgramData` and ship the `--audit` CLI mode for repeatable measurement. The acceptance test enforces >= 95 % concordance with the human verdicts (within 1 RiskLevel step) AND zero false-Supprimer recommendations on items the human marked Critique. The `--audit` mode lets the user re-run the analysis on any machine and produce a CSV for offline review.

Purpose: Without this corpus test, "Phase 10 reduces FP from > 90 % to < 5 %" is just a claim. This plan turns it into a CI gate. The CSV mode also unblocks future iterations — the user can run `--audit` after rule changes and diff the output.
Output: 1 corpus fixture (365 items) + 1 acceptance test class + 1 CSV writer service + 1 modified App.xaml.cs (CLI parsing). ~6 new tests. Suite total ~200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md
@.planning/phases/10-orphan-detection-precision-refactor/10-01-SUMMARY.md
@.planning/phases/10-orphan-detection-precision-refactor/10-02-SUMMARY.md
@.planning/phases/10-orphan-detection-precision-refactor/10-03-SUMMARY.md
@.planning/phases/10-orphan-detection-precision-refactor/10-04-SUMMARY.md
@CLAUDE.md
@src/DiskScout.App/Helpers/AppPaths.cs
@src/DiskScout.App/Services/AppDataOrphanPipeline.cs
@src/DiskScout.App/Services/OrphanDetectorService.cs
@src/DiskScout.App/App.xaml.cs

<interfaces>
<!-- Contract this plan creates. -->

CorpusItem (test-only, in CorpusAcceptanceTests.cs):

```csharp
private sealed record CorpusItem(
    int Index,                      // 1..365 from the original audit
    string Path,                     // e.g. "C:\\ProgramData\\NinjaRMMAgent"
    long SizeBytes,
    DateTime LastWriteUtc,           // approximate; ISO-8601 in JSON
    string Verdict,                  // "Aucun" | "Faible" | "Moyen" | "Eleve" | "Critique"
    string RecommendedAction);       // "Supprimer" | "CorbeilleOk" | "VerifierAvant" | "NePasToucher"
```

IAuditCsvWriter.cs / AuditCsvWriter.cs (NEW):

```csharp
namespace DiskScout.Services;
using DiskScout.Models;

public interface IAuditCsvWriter
{
    Task<string> WriteAsync(
        IEnumerable<AppDataOrphanCandidate> candidates,
        CancellationToken cancellationToken = default);
    // Returns the absolute path to the written CSV.
}
```
</interfaces>

<corpus_source>
The 365-item corpus is described in 10-CONTEXT.md `<specifics>` (and was provided in the user's original audit transcript that produced CONTEXT.md). The corpus was collected by manual audit on `C:\ProgramData` of an HP corporate machine running NinjaRMM + Zscaler + Matrix42 + Empirum + Bitdefender + Splashtop + Autodesk + Office 365 + Teams + Claude Desktop + Python Manager + WhatsApp UWP.

Distribution measured by the human auditor:
- ~71 % CRITIQUE (composants OS + caches MSI + credentials)
- ~12 % ELEVE (corporate agents, drivers, applications actives)
- ~10 % MOYEN (caches VC++ Redist)
- ~6 % FAIBLE (logs vides)
- ~1 % AUCUN (vrais résidus)

All paths begin with `C:\ProgramData\` except item [14] which is `C:\Users\tanguy.delrieu\AppData\LocalLow`.

**The executor of this plan must extract the full 365-item list from the original audit transcript embedded in the conversation that produced CONTEXT.md.** If that transcript is not available in the current conversation, the executor MUST instead generate a SYNTHETIC corpus matching the documented distribution (260 Critique items spanning the OS-critical and PackageCache categories from Plan 10-01's JSONs, 44 Eleve items spanning corporate-agent + driver-data, 36 Moyen items spanning vendor-shared, 22 Faible items, 3 Aucun items) and clearly label it `synthesized: true` in the JSON metadata header so the user knows to swap in their real audit later. The acceptance thresholds (>= 95 % concordance, 0 % Critique-to-Supprimer) apply to whichever corpus is shipped.
</corpus_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize the 365-item corpus fixture + write the acceptance test reaching >= 95% concordance</name>
  <files>
    tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json (NEW),
    tests/DiskScout.Tests/CorpusAcceptanceTests.cs (NEW),
    tests/DiskScout.Tests/DiskScout.Tests.csproj (MODIFY — add EmbeddedResource for the fixture)
  </files>
  <read_first>
    - .planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md `<specifics>` (corpus distribution + scoring rules + path examples)
    - src/DiskScout.App/Services/AppDataOrphanPipeline.cs (the engine under test)
    - src/DiskScout.App/Services/PathRuleEngine.cs (loaded with 5 JSONs from 10-01)
    - tests/DiskScout.Tests/AppDataOrphanPipelineTests.cs (10-04 — uses fakes; this plan uses REAL implementations end-to-end)
    - tests/DiskScout.Tests/DiskScout.Tests.csproj (current EmbeddedResource pattern, if any)
    - The user's original audit transcript embedded in the conversation that produced CONTEXT.md (PRIMARY source of the 365 items + per-item human verdict). If unavailable, synthesize per the `<corpus_source>` block.
  </read_first>
  <behavior>
    - `tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json` is a JSON array of 365 items, each with: `index`, `path`, `sizeBytes`, `lastWriteUtc` (ISO-8601), `verdict` (one of the 5 RiskLevel strings), `recommendedAction` (one of the 4 RecommendedAction strings). Optionally a top-level metadata header documenting `corpusVersion`, `synthesized` (true/false), `sourceMachine` (anonymized).
    - The fixture is shipped as `<EmbeddedResource>` inside `tests/DiskScout.Tests/DiskScout.Tests.csproj` (so the test reads it via `Assembly.GetExecutingAssembly().GetManifestResourceStream`). LogicalName: `DiskScout.Tests.Fixtures.programdata_corpus_365.json`.
    - `CorpusAcceptanceTests.RemanentDetector_Should_Match_Manual_Audit`:
      a. Load the corpus JSON.
      b. Build a real `AppDataOrphanPipeline` using the actual `PathRuleEngine` (loads embedded JSONs), `ParentContextAnalyzer`, `MachineSnapshotProvider` with HAND-FAKED enumerators (snapshot fixture should reflect a corporate machine with NinjaRMM, Bitdefender, Zscaler services + Intel/NVIDIA drivers + a few Appx packages — DO NOT call the real OS enumerators in tests; freeze the machine state for deterministic CI).
      c. For each corpus item, construct a synthetic `FileSystemNode` (Id arbitrary, Kind=Directory, FullPath, SizeBytes, LastModifiedUtc) and an `IReadOnlyList<InstalledProgram>` containing 30-50 fake programs covering the corporate stack (NinjaRMM Agent, Zscaler Client Connector, Matrix42 Agent, Bitdefender, Splashtop, Office 365, Teams, Claude Desktop, Python, etc.).
      d. Call `pipeline.EvaluateAsync(node, programs, CT.None).Result` for every corpus item. Pipeline returns null for HardBlacklist hits → these count as "verdict=Critique, action=NePasToucher" (the hard suppression IS the most-conservative classification).
      e. Compute concordance: an item matches if `Math.Abs((int)engine.Risk - (int)humanVerdict) <= 1` (within one RiskLevel step). The test asserts `concordance >= 0.95`.
      f. Compute the critical-misclassification rate: count items where `humanVerdict == Critique` AND `engine.Action ∈ {Supprimer, CorbeilleOk}`. The test asserts `criticalMisclassifications == 0`.
      g. On failure, the test message lists the first 10 mismatches with their path + human verdict + engine output for triage.
    - The acceptance test is permitted to take up to 30 seconds (375 pipeline calls × ~50 ms each = 18 s typical). Mark with `[Trait("Category", "Acceptance")]` so it can be filtered out for fast CI cycles if needed.
  </behavior>
  <action>
    1. Generate the corpus JSON. PREFERRED: extract the 365 items from the user's original audit transcript that produced CONTEXT.md (search the conversation history for items numbered 1..365 with paths under `C:\ProgramData\`). FALLBACK: synthesize per the `<corpus_source>` block, marking `"synthesized": true` in a top-level metadata object.
    2. Write `tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json` with at least 365 items in the array. Validate the JSON parses cleanly (`jq . file.json` if available, or a manual `JsonDocument.Parse` round-trip in the test).
    3. Modify `tests/DiskScout.Tests/DiskScout.Tests.csproj`. Add an `<ItemGroup>` with `<EmbeddedResource Include="Fixtures\programdata_corpus_365.json"><LogicalName>DiskScout.Tests.Fixtures.programdata_corpus_365.json</LogicalName></EmbeddedResource>`.
    4. Create `tests/DiskScout.Tests/CorpusAcceptanceTests.cs`. Implement the test per `<behavior>` step-by-step. Use the project's hand-written-fakes pattern (no Moq) for the `IServiceEnumerator`, `IDriverEnumerator`, `IAppxEnumerator`, `IScheduledTaskEnumerator` fakes. Hard-code a representative corporate-machine snapshot (NinjaRMMAgent service, Bitdefender service, Zscaler service, Matrix42, Empirum, Splashtop services + Intel HD Graphics + NVIDIA drivers + WhatsAppUWP appx + a few Microsoft Teams scheduled tasks). The fake list of installed programs should similarly mirror that machine.
    5. Run the test in isolation: `dotnet test --filter "FullyQualifiedName~CorpusAcceptance"`. Expect PASS — concordance >= 95 %, critical misclassifications = 0. If FAIL, the most likely root causes are: (a) PathRule patterns from 10-01 don't cover an OS-critical path the corpus exercises (fix: add to `os-critical.json`), (b) MinRiskFloor missing on a category (fix: in `package-cache.json` or `corporate-agent.json`), (c) ScoreDelta wrong (fix in 10-04's `ConfidenceScorer.cs`). The fix path is to amend the EARLIER plan's JSON or scorer — cite the diff in the SUMMARY but DO NOT silently relax the threshold.
    6. Run the FULL test suite. Expect ~200 passing.
  </action>
  <verify>
    <automated>dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj --filter "FullyQualifiedName~CorpusAcceptance" --nologo --verbosity minimal 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json` exits `0`.
    - Corpus item count: `grep -cE "\"index\":" tests/DiskScout.Tests/Fixtures/programdata_corpus_365.json` returns `365` (or use `jq '. | length'` if available; the `index` field is unique per item).
    - `grep -c "EmbeddedResource Include=\"Fixtures\\\\programdata_corpus_365" tests/DiskScout.Tests/DiskScout.Tests.csproj` returns `1`.
    - `grep -c "RemanentDetector_Should_Match_Manual_Audit" tests/DiskScout.Tests/CorpusAcceptanceTests.cs` returns `1`.
    - `grep -c "concordance >= 0.95\\|>= 0\\.95" tests/DiskScout.Tests/CorpusAcceptanceTests.cs` returns at least `1` (threshold encoded).
    - `grep -c "Critique" tests/DiskScout.Tests/CorpusAcceptanceTests.cs` returns at least `1` (zero-misclassification check encoded).
    - `dotnet test --filter "FullyQualifiedName~CorpusAcceptance"` exits `0` with `Passed!` line.
    - Full suite `dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj` exits `0` with `Passed:` count >= 200.
  </acceptance_criteria>
  <done>The precision claim is now a CI gate. Suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --audit CLI mode + AuditCsvWriter + tests</name>
  <files>
    src/DiskScout.App/Services/IAuditCsvWriter.cs (NEW),
    src/DiskScout.App/Services/AuditCsvWriter.cs (NEW),
    src/DiskScout.App/App.xaml.cs (MODIFY — parse --audit, route to headless flow),
    tests/DiskScout.Tests/AuditCsvWriterTests.cs (NEW)
  </files>
  <read_first>
    - src/DiskScout.App/App.xaml.cs (current OnStartup — line 30 onwards)
    - src/DiskScout.App/Helpers/AppPaths.cs (AuditFolder from 10-01)
    - src/DiskScout.App/Models/AppDataOrphanCandidate.cs (10-04)
    - src/DiskScout.App/Services/AppDataOrphanPipeline.cs (10-04)
    - .planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md `<decisions>` Mode --audit section
  </read_first>
  <behavior>
    - `AuditCsvWriter.WriteAsync` writes a CSV with header row: `Path,SizeBytes,ConfidenceScore,RiskLevel,TriggeredRules,ExonerationRules,RecommendedAction`. Each candidate becomes one row. `TriggeredRules` is a semicolon-joined list of `RuleHit.RuleId` strings (e.g., `"os-critical-system32;package-cache-vcredist"`). `ExonerationRules` is the semicolon-joined list of `MatcherHit.Source:Evidence` strings (these REDUCE the score from 100 — they're the reasons the candidate is NOT a residue, hence "exoneration"). UTF-8 BOM-prefixed file (Excel reads it cleanly), CRLF line endings, double-quote-and-escape any field containing comma/quote/newline (RFC-4180 minimal compliance).
    - Output path: `Path.Combine(AppPaths.AuditFolder, $"audit_{DateTime.Now:yyyyMMdd_HHmmss}.csv")`. Returns the path string.
    - `App.xaml.cs.OnStartup` parses `e.Args` for `--audit` flag (case-insensitive). When present:
      a. Skip `MainWindow.Show()`.
      b. Run a synchronous (`.GetAwaiter().GetResult()`) headless flow: pick the first fixed local drive via `IDriveService`, run `IFileSystemScanner.ScanAsync`, run `IInstalledProgramsScanner.ScanAsync`, run `IOrphanDetectorService.DetectAsync` (which now goes through the Phase 10 pipeline for AppData), filter `orphans` to those with `category == AppDataOrphan && Diagnostics != null`, project to their `Diagnostics` field (the AppDataOrphanCandidate), pass to `IAuditCsvWriter.WriteAsync`.
      c. Print the CSV path to stdout via `Console.WriteLine` (visible if launched from a console).
      d. Call `Shutdown(0)` to exit the WPF app cleanly.
      e. On any exception, log Fatal + `Shutdown(1)`.
    - Because `--audit` is intended to be invoked from a Powershell console, `App.xaml.cs.OnStartup` must call `AttachConsole(-1)` (P/Invoke to `kernel32!AttachConsole`) early, BEFORE writing to stdout — otherwise the WPF app has no console. If `AttachConsole` returns false (no parent console), fall back to writing the path to a separate `audit_path.txt` next to the CSV.
  </behavior>
  <action>
    1. Create `src/DiskScout.App/Services/IAuditCsvWriter.cs` with the interface from `<interfaces>`.
    2. Create `src/DiskScout.App/Services/AuditCsvWriter.cs`. Public sealed class. Constructor takes `ILogger`. Implement `WriteAsync` per `<behavior>`. Use `StreamWriter` with `Encoding.UTF8` (BOM included by default with `Encoding.UTF8`). Helper method `EscapeCsv(string s)` handles RFC-4180 escaping. NO `File.Delete`. The file is created via `File.Create` / `StreamWriter` write — that's fine (creating files is allowed; only delete is forbidden by CLAUDE.md).
    3. Modify `src/DiskScout.App/App.xaml.cs`:
       - At the top of `OnStartup`, after `_logger` setup but before service composition, add: `bool isAuditMode = e.Args.Any(a => string.Equals(a, "--audit", StringComparison.OrdinalIgnoreCase));`.
       - Move the existing service composition and `mainWindow.Show()` into a guarded block: `if (isAuditMode) { RunHeadlessAuditAsync(...).GetAwaiter().GetResult(); Shutdown(0); return; } else { /* existing UI flow */ }`.
       - Implement `private async Task RunHeadlessAuditAsync(...)` private method composing the same services + running the headless scan + writing CSV. Reuse the existing service constructors — composition is the same as the UI path, just no window.
       - At the very top of `OnStartup` (before `base.OnStartup(e)` even, to capture stdout if launched from console): P/Invoke `[DllImport("kernel32.dll")] static extern bool AttachConsole(int dwProcessId);` and call `AttachConsole(-1)` ONLY if `isAuditMode` is true.
       - On audit-mode exception: `Console.Error.WriteLine($"Audit failed: {ex.Message}"); Shutdown(1); return;`.
    4. Create `tests/DiskScout.Tests/AuditCsvWriterTests.cs`. Cover: (a) header row is exactly the 7 expected columns, (b) a candidate with TriggeredRules ["a","b"] produces field `"a;b"`, (c) a candidate with a path containing comma is double-quoted, (d) UTC timestamp in filename matches `audit_YYYYMMDD_HHmmss.csv` regex pattern, (e) returned path equals `Path.Combine(AppPaths.AuditFolder, expectedFileName)`. Use a temp folder via `AppPaths.AuditFolder` (it returns the real %LocalAppData%\DiskScout\audits — acceptable in tests, just clean up the file after).
    5. Run full suite. Expect ~205 passing.
    6. Auto-launch validation: per user MEMORY.md, after build succeeds, run the headless mode: `dotnet run --project src/DiskScout.App/DiskScout.App.csproj -- --audit`. Confirm: process exits 0 within ~15-30 s (depending on disk size); a CSV appears at `%LocalAppData%\DiskScout\audits\audit_*.csv` with at least the header row + a few real rows. NO need to inspect every line — existence + header presence + non-empty body is sufficient. Document the launch outcome in 10-05-SUMMARY.md.
  </action>
  <verify>
    <automated>dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj --nologo --verbosity minimal 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public sealed class AuditCsvWriter : IAuditCsvWriter" src/DiskScout.App/Services/AuditCsvWriter.cs` returns `1`.
    - `grep -c "Path,SizeBytes,ConfidenceScore,RiskLevel,TriggeredRules,ExonerationRules,RecommendedAction" src/DiskScout.App/Services/AuditCsvWriter.cs` returns `1` (header literal).
    - `grep -c "audit_" src/DiskScout.App/Services/AuditCsvWriter.cs` returns at least `1` (filename pattern).
    - `grep -c "AppPaths.AuditFolder" src/DiskScout.App/Services/AuditCsvWriter.cs` returns at least `1`.
    - `grep -c "isAuditMode\\|--audit" src/DiskScout.App/App.xaml.cs` returns at least `2`.
    - `grep -c "RunHeadlessAuditAsync\\|HeadlessAudit" src/DiskScout.App/App.xaml.cs` returns at least `1`.
    - `grep -c "AttachConsole" src/DiskScout.App/App.xaml.cs` returns at least `1`.
    - `grep -c "public class AuditCsvWriterTests" tests/DiskScout.Tests/AuditCsvWriterTests.cs` returns `1`.
    - `grep -cE "\\[Fact\\]|\\[Theory\\]" tests/DiskScout.Tests/AuditCsvWriterTests.cs` returns at least `5`.
    - `dotnet test` exits `0` with `Passed:` count >= 205.
    - NO `File.Delete` / `Directory.Delete` introduced: `grep -rE "File\\.Delete|Directory\\.Delete" src/DiskScout.App/Services/AuditCsvWriter.cs src/DiskScout.App/App.xaml.cs` returns no results in the new code blocks (the existing `PurgeOldScansAsync` in App.xaml.cs uses `f.Delete()` — that's pre-existing, NOT introduced by this plan; verify with `git diff` that the line isn't a new addition).
  </acceptance_criteria>
  <done>--audit mode operational; CSV writer functional; tests green; user can re-run on any machine.</done>
</task>

</tasks>

<verification>
1. `dotnet build` exits `0`.
2. `dotnet test` exits `0` with `Passed:` count >= 205 (including the corpus acceptance test).
3. CorpusAcceptanceTests passes — concordance >= 95 %, zero Critique misclassifications.
4. `dotnet run -- --audit` produces a CSV at `%LocalAppData%\DiskScout\audits\audit_*.csv` and exits `0`.
5. NO `File.Delete` / `Directory.Delete` ADDED in this plan's diff.
</verification>

<success_criteria>
- 365-item corpus fixture shipped as embedded resource.
- Acceptance test pinning >= 95 % concordance + 0 % Critique misclassifications.
- `--audit` CLI mode functional with headless WPF + console attach + CSV output.
- ~6 new tests; suite total ~205.
- 2 atomic commits: `test(10-05): 365-item corpus fixture + acceptance test (>=95% concordance, 0% Critique misclass)`, `feat(10-05): --audit headless CLI mode + AuditCsvWriter`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-orphan-detection-precision-refactor/10-05-SUMMARY.md`. Document:
- Corpus origin (extracted from user transcript vs synthesized + reason)
- Measured concordance percentage (e.g., "97.3 % within 1 step")
- Misclassification count (Critique → Supprimer/CorbeilleOk)
- CSV header schema
- Auto-launch outcome of `--audit` mode (file path, row count, time elapsed)
- Top 5 corpus items where engine and human disagree (informational; should not be Critique→Aucun mismatches)
</output>
