---
phase: 10-orphan-detection-precision-refactor
plan: 06
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - src/DiskScout.App/ViewModels/OrphansViewModel.cs
  - src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs
  - src/DiskScout.App/Views/Tabs/OrphansTabView.xaml
  - src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml
  - src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml.cs
  - src/DiskScout.App/Helpers/RiskLevelToBrushConverter.cs
  - docs/heuristics.md
  - tests/DiskScout.Tests/OrphanDiagnosticsViewModelTests.cs
autonomous: false
requirements: []
must_haves:
  truths:
    - "OrphansTabView shows a Score badge column for AppData orphans (color-coded by RiskLevel)"
    - "Hovering the score badge shows a tooltip listing TriggeredRules and matched signals"
    - "Right-click menu or button 'Pourquoi ?' opens a diagnostics window detailing the score breakdown step by step"
    - "Non-AppData orphan rows show no badge (Diagnostics is null)"
    - "docs/heuristics.md documents the 7-step pipeline + scoring rules + path rule categories"
  artifacts:
    - path: "src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml"
      provides: "Modal showing per-candidate full diagnostic trace"
      contains: "OrphanDiagnosticsWindow"
    - path: "src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs"
      provides: "VM exposing the AppDataOrphanCandidate fields formatted for the modal"
      contains: "OrphanDiagnosticsViewModel"
    - path: "docs/heuristics.md"
      provides: "User-facing documentation of the 7-step pipeline + scoring + path rules"
      contains: "## Pipeline en 7 étapes"
  key_links:
    - from: "src/DiskScout.App/Views/Tabs/OrphansTabView.xaml"
      to: "OrphanRow.Diagnostics + RiskLevelToBrushConverter"
      via: "DataTemplate column with conditional Visibility on Diagnostics != null"
      pattern: "Diagnostics"
    - from: "src/DiskScout.App/ViewModels/OrphansViewModel.cs"
      to: "OrphanCandidate.Diagnostics"
      via: "OrphanRow ctor copies Diagnostics into a new public property"
      pattern: "Diagnostics"
---

<objective>
Surface Phase 10's diagnostics in the Rémanents tab UI: a color-coded Score badge column, a tooltip listing the rules that fired, and a "Pourquoi ?" modal showing the step-by-step score breakdown. Also ship `docs/heuristics.md` as a reference for the user (and for future contributors).

Purpose: Without this UI surface, all the precision work from Plans 10-01..10-05 is invisible — the user sees the same Rémanents list as before but with fewer entries. The Score column + Pourquoi modal lets the user trust the engine's recommendations and override them when their context differs from the heuristics.
Output: 1 modified VM + 1 new VM + 1 modified XAML + 1 new modal + 1 IValueConverter + 1 docs file + ~5 tests + 1 visual UAT checkpoint. Suite total ~210.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md
@.planning/phases/10-orphan-detection-precision-refactor/10-04-SUMMARY.md
@CLAUDE.md
@src/DiskScout.App/ViewModels/OrphansViewModel.cs
@src/DiskScout.App/Views/Tabs/OrphansTabView.xaml
@src/DiskScout.App/Models/AppDataOrphanCandidate.cs
@src/DiskScout.App/Models/OrphanCandidate.cs
@src/DiskScout.App/Models/PathRule.cs

<interfaces>
<!-- New VM + converter contracts. -->

OrphanDiagnosticsViewModel.cs (NEW — sealed partial ObservableObject):

```csharp
namespace DiskScout.ViewModels;
using DiskScout.Models;

public sealed partial class OrphanDiagnosticsViewModel : ObservableObject
{
    public string FullPath { get; }
    public string ParentSignificantPath { get; }
    public int ConfidenceScore { get; }
    public RiskLevel Risk { get; }
    public string RiskLabel => Risk.ToString();
    public RecommendedAction Action { get; }
    public string ActionLabel => Action.ToString();
    public string CategoryLabel { get; }                    // PathCategory.ToString()
    public IReadOnlyList<string> TriggeredRulesLines { get; } // formatted "RuleId — Reason"
    public IReadOnlyList<string> MatchedSourcesLines { get; } // formatted "Source: Evidence (-N pts)"
    public string SizeDisplay { get; }
    public string LastWriteDisplay { get; }
    public OrphanDiagnosticsViewModel(AppDataOrphanCandidate diag);
}
```

RiskLevelToBrushConverter.cs (NEW IValueConverter):

```csharp
namespace DiskScout.Helpers;
using System.Windows.Media;
public sealed class RiskLevelToBrushConverter : System.Windows.Data.IValueConverter { ... }
// Aucun=Green, Faible=Lime, Moyen=Orange, Eleve=DarkOrange, Critique=Red
// Reuse palette from Views/Tabs/HealthTabView.xaml if present, else hard-code.
```

OrphanRow extension (in OrphansViewModel.cs MODIFY):

```csharp
public sealed partial class OrphanRow : ObservableObject
{
    // existing fields unchanged
    public AppDataOrphanCandidate? Diagnostics { get; }    // NEW — copied from candidate ctor

    public bool HasDiagnostics => Diagnostics is not null; // NEW — for binding visibility
    public int? ConfidenceScore => Diagnostics?.ConfidenceScore;
    public RiskLevel? Risk => Diagnostics?.Risk;
    public string? ScoreBadgeText => Diagnostics is null ? null : $"{Diagnostics.ConfidenceScore}";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend OrphansViewModel + add OrphanDiagnosticsViewModel + RiskLevelToBrushConverter + tests</name>
  <files>
    src/DiskScout.App/ViewModels/OrphansViewModel.cs (MODIFY — extend OrphanRow with Diagnostics fields),
    src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs (NEW),
    src/DiskScout.App/Helpers/RiskLevelToBrushConverter.cs (NEW),
    tests/DiskScout.Tests/OrphanDiagnosticsViewModelTests.cs (NEW)
  </files>
  <read_first>
    - src/DiskScout.App/ViewModels/OrphansViewModel.cs (full file — focus on `OrphanRow` ~line 371 onward)
    - src/DiskScout.App/Models/AppDataOrphanCandidate.cs (10-04)
    - src/DiskScout.App/Models/OrphanCandidate.cs (10-04 — Diagnostics property)
    - src/DiskScout.App/Models/PathRule.cs (RiskLevel + RecommendedAction enums)
    - tests/DiskScout.Tests/OrphansViewModelTests.cs IF it exists (else just write fresh tests)
  </read_first>
  <behavior>
    - `OrphanRow` ctor (currently lines 371-379) gains a single line: `Diagnostics = c.Diagnostics;` after the existing Category assignment. The `Diagnostics` property is `public AppDataOrphanCandidate? Diagnostics { get; }`. The `HasDiagnostics`, `ConfidenceScore`, `Risk`, `ScoreBadgeText` are computed properties as in `<interfaces>`. NO breaking changes to existing OrphanRow API — just additions.
    - `OrphanDiagnosticsViewModel` is a sealed partial ObservableObject (no [ObservableProperty] needed since all properties are read-only — set in ctor). Pre-formats `TriggeredRulesLines` as `$"{rule.RuleId} — {rule.Reason}"` and `MatchedSourcesLines` as `$"{hit.Source}: {hit.Evidence} ({hit.ScoreDelta:+#;-#;0} pts)"`. `SizeDisplay` reuses the byte-formatting helper found at the bottom of `OrphansViewModel.cs` (extract to a static helper if not already public). `LastWriteDisplay` uses `LastWriteUtc.ToLocalTime().ToString("yyyy-MM-dd HH:mm")`.
    - `RiskLevelToBrushConverter` returns `SolidColorBrush` per RiskLevel: Aucun=`#27AE60`, Faible=`#2ECC71`, Moyen=`#F39C12`, Eleve=`#E67E22`, Critique=`#E74C3C`. Also a sibling `RiskLevelToTextConverter` is NOT needed — XAML uses `{Binding Risk}` directly with a `StringFormat` if needed.
  </behavior>
  <action>
    1. Modify `src/DiskScout.App/ViewModels/OrphansViewModel.cs`. In the `OrphanRow` class (~lines 371-401), after `Category = c.Category;`, add `Diagnostics = c.Diagnostics;`. Add the public read-only property `Diagnostics`. Add the 4 computed properties (`HasDiagnostics`, `ConfidenceScore`, `Risk`, `ScoreBadgeText`).
    2. Create `src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs`. Namespace `DiskScout.ViewModels`. `using DiskScout.Models;`. Implement per `<interfaces>`. Constructor takes `AppDataOrphanCandidate` and populates all properties; on null fields (e.g., empty TriggeredRules), populate with `Array.Empty<string>()`.
    3. Create `src/DiskScout.App/Helpers/RiskLevelToBrushConverter.cs`. Namespace `DiskScout.Helpers`. `IValueConverter`. The 5-color palette as in `<behavior>`. ConvertBack throws `NotSupportedException` (one-way binding only).
    4. Create `tests/DiskScout.Tests/OrphanDiagnosticsViewModelTests.cs`. Cover: (a) ctor with a real AppDataOrphanCandidate populates all properties non-null, (b) TriggeredRulesLines formatted as `"{Id} — {Reason}"`, (c) MatchedSourcesLines formatted as `"{Source}: {Evidence} (-N pts)"` with negative-format integer, (d) SizeDisplay for 0 returns `"0 o"`, (e) RiskLabel returns the enum name. Add 1-2 tests for `RiskLevelToBrushConverter` (input Aucun → green brush; input Critique → red brush).
    5. `dotnet build` + `dotnet test`.
  </action>
  <verify>
    <automated>dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj --filter "FullyQualifiedName~OrphanDiagnostics" --nologo --verbosity minimal 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Diagnostics = c.Diagnostics" src/DiskScout.App/ViewModels/OrphansViewModel.cs` returns `1`.
    - `grep -c "public AppDataOrphanCandidate\\? Diagnostics" src/DiskScout.App/ViewModels/OrphansViewModel.cs` returns at least `1`.
    - `grep -c "public sealed partial class OrphanDiagnosticsViewModel" src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs` returns `1`.
    - `grep -c "public sealed class RiskLevelToBrushConverter" src/DiskScout.App/Helpers/RiskLevelToBrushConverter.cs` returns `1`.
    - `grep -c "public class OrphanDiagnosticsViewModelTests" tests/DiskScout.Tests/OrphanDiagnosticsViewModelTests.cs` returns `1`.
    - `dotnet test --filter "FullyQualifiedName~OrphanDiagnostics"` exits `0`.
  </acceptance_criteria>
  <done>The VM layer is ready; XAML in Task 2 will bind to it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Score column + tooltip + "Pourquoi ?" button to OrphansTabView + create OrphanDiagnosticsWindow</name>
  <files>
    src/DiskScout.App/Views/Tabs/OrphansTabView.xaml (MODIFY — add Score column + tooltip + button),
    src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml (NEW),
    src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml.cs (NEW — minimal code-behind),
    src/DiskScout.App/App.xaml (MODIFY — register RiskLevelToBrushConverter as resource)
  </files>
  <read_first>
    - src/DiskScout.App/Views/Tabs/OrphansTabView.xaml (current row template ~lines 137-164)
    - src/DiskScout.App/Views/UninstallWizard/UninstallWizardWindow.xaml (modal pattern reference from Phase 9)
    - src/DiskScout.App/App.xaml (Application.Resources for converter registration)
    - src/DiskScout.App/ViewModels/OrphanDiagnosticsViewModel.cs (just created)
    - .planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md `<decisions>` UI section
  </read_first>
  <behavior>
    - The existing row template in `OrphansTabView.xaml` (lines 137-164) is a 3-column Grid: checkbox / FullPath+Reason stacked / SizeDisplay. We add a 4TH column BEFORE SizeDisplay containing a score badge, ONLY visible when `HasDiagnostics=true`. The badge is a `Border` with `Background={Binding Risk, Converter={StaticResource RiskLevelToBrushConverter}}`, CornerRadius=8, Padding="6,2", containing a `TextBlock Text="{Binding ScoreBadgeText}"` foreground=White FontWeight=SemiBold. Visibility binds on `HasDiagnostics` via `BooleanToVisibilityConverter`. Tooltip on the badge displays a multi-line string built from TriggeredRules + MatchedSources via a value converter or a multi-binding StringFormat.
    - A "Pourquoi ?" `Button` is added to the right of the SizeDisplay column (column 5), Visibility bound on `HasDiagnostics`, Click handler opens `OrphanDiagnosticsWindow` with `DataContext = new OrphanDiagnosticsViewModel(row.Diagnostics!)`. The button is small (Padding="4,1"), Content="Pourquoi ?", Style minimal.
    - `OrphanDiagnosticsWindow.xaml`: a `Window` (not a UserControl) sized 700x500, modal (set `Owner` in code-behind), title `"Pourquoi cette détection ?"`. Layout: 5-row Grid (header with full path / score badge + risk label / category + significant parent / triggered rules ListBox / matched sources ListBox / close button). Bindings on the VM properties.
    - `OrphanDiagnosticsWindow.xaml.cs`: minimal code-behind. Constructor takes no args; `InitializeComponent()`. Single Click handler `OnCloseClicked` calling `Close()`.
    - `App.xaml.Application.Resources` adds `<helpers:RiskLevelToBrushConverter x:Key="RiskLevelToBrushConverter"/>` (with appropriate `xmlns:helpers="clr-namespace:DiskScout.Helpers"`).
  </behavior>
  <action>
    1. Modify `src/DiskScout.App/App.xaml` to register the converter resource. Add the `xmlns:helpers` attribute if absent. Inside `<Application.Resources>` (or the existing `ResourceDictionary`), add `<helpers:RiskLevelToBrushConverter x:Key="RiskLevelToBrushConverter"/>`.
    2. Modify `src/DiskScout.App/Views/Tabs/OrphansTabView.xaml`. Locate the row template at lines ~143-160 (the inner `<Grid>` with 3 columns). Modify it to 5 columns: checkbox / path+reason / score badge (new) / SizeDisplay / Pourquoi button (new). Use the badge styling from `<behavior>`. Use `Visibility="{Binding HasDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}"` on both the score column AND the Pourquoi button (so non-AppData rows show neither).
    3. Build the multi-line tooltip via a small `StringFormat` or via inline run binding. Acceptable simple form: tooltip text = `"Score: {ConfidenceScore}/100 — Risque: {RiskLabel}\n{first triggered rule}\n{first matched source}"`. If formatting cleanly is hard, use a simple ToolTip on the badge: `<Border.ToolTip><TextBlock Text="{Binding Diagnostics.Reason}" /></Border.ToolTip>`.
    4. Wire the "Pourquoi ?" button click. Two options: (a) RelayCommand on `OrphansViewModel` with the row as parameter — preferred for MVVM strictness — `[RelayCommand] private void ShowDiagnostics(OrphanRow? row) { if (row?.Diagnostics is null) return; var vm = new OrphanDiagnosticsViewModel(row.Diagnostics); var w = new OrphanDiagnosticsWindow { DataContext = vm, Owner = Application.Current.MainWindow }; w.ShowDialog(); }`. (b) Code-behind in `OrphansTabView.xaml.cs` — only acceptable per CLAUDE.md if "événements UI purs"; opening a dialog is borderline — prefer (a). Choose (a).
    5. Create `src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml`. Window with the 5-row layout from `<behavior>`. Use existing styles from `App.xaml` (Background, ForegroundBrush, etc.). Bind `ListBox.ItemsSource` to `TriggeredRulesLines` and `MatchedSourcesLines`. Close button at the bottom right.
    6. Create `src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml.cs`. `public partial class OrphanDiagnosticsWindow : Window { public OrphanDiagnosticsWindow() { InitializeComponent(); } private void OnCloseClicked(object sender, System.Windows.RoutedEventArgs e) => Close(); }`.
    7. Run `dotnet build` to ensure the XAML compiles.
    8. Auto-launch (per user MEMORY.md): `dotnet run --project src/DiskScout.App/DiskScout.App.csproj`. Trigger a scan of `C:\` (or whichever fixed drive is fastest). Manually verify: (a) the Rémanents tab shows AppData orphan rows with score badges color-coded, (b) hovering a badge shows a tooltip, (c) clicking "Pourquoi ?" opens a modal with the 7-step trace, (d) non-AppData orphans (StaleTemp, BrowserCache, etc.) show NO badge and NO Pourquoi button. Document the launch outcome in 10-06-SUMMARY.md.
  </action>
  <verify>
    <automated>dotnet build src/DiskScout.App/DiskScout.App.csproj -c Debug --nologo 2>&amp;1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RiskLevelToBrushConverter" src/DiskScout.App/App.xaml` returns at least `1` (registered as resource).
    - `grep -c "ScoreBadgeText\\|Diagnostics" src/DiskScout.App/Views/Tabs/OrphansTabView.xaml` returns at least `2` (badge column + button visibility).
    - `grep -c "Pourquoi" src/DiskScout.App/Views/Tabs/OrphansTabView.xaml` returns at least `1`.
    - `grep -c "HasDiagnostics" src/DiskScout.App/Views/Tabs/OrphansTabView.xaml` returns at least `2` (visibility on badge + button).
    - `ls src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml` exits `0`.
    - `grep -c "OrphanDiagnosticsWindow" src/DiskScout.App/Views/OrphanDiagnosticsWindow.xaml.cs` returns at least `1`.
    - `grep -c "ShowDiagnosticsCommand\\|ShowDiagnostics" src/DiskScout.App/ViewModels/OrphansViewModel.cs` returns at least `1` (RelayCommand wired).
    - `dotnet build src/DiskScout.App/DiskScout.App.csproj -c Debug --nologo` exits `0`.
  </acceptance_criteria>
  <done>UI surface complete. Visual UAT pending in Task 4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author docs/heuristics.md (user-facing reference)</name>
  <files>
    docs/heuristics.md (NEW)
  </files>
  <read_first>
    - .planning/phases/10-orphan-detection-precision-refactor/10-CONTEXT.md (decisions + specifics — full file)
    - .planning/phases/10-orphan-detection-precision-refactor/10-04-SUMMARY.md (final scoring weights post-implementation, may differ slightly from CONTEXT.md if 10-04 had to tune)
    - src/DiskScout.App/Resources/PathRules/os-critical.json (the actual deployed PathRule patterns)
    - src/DiskScout.App/Resources/PathRules/aliases.json (the actual aliases)
  </read_first>
  <behavior>
    - `docs/heuristics.md` is a single Markdown file aimed at the user (and future contributors) explaining: (1) what changed in Phase 10 (vs. the original "fuzzy + threshold 0.7" Phase 3 heuristic), (2) the 7-step pipeline diagram in ASCII art, (3) the score deltas per matcher / path category / bonus, (4) the Risk → Action mapping table, (5) the HardBlacklist + MinRiskFloor invariants, (6) how to add a custom path rule (place a JSON in `%LocalAppData%\DiskScout\path-rules\`, JSON shape example, last-write-wins), (7) how to extend aliases (NOT possible in v1.2 — aliases.json is embedded only; future v1.3 will add user-aliases support), (8) FAQ: "I marked X as Aucun but Phase 10 says Critique — how to override?" (answer: write a user PathRule with that path and `category: "Generic"` to bypass the embedded rule), (9) reference to the corpus + concordance metric.
    - File length target: 200-400 lines. Tone: friendly, French-leaning (project is French — mirror the in-app strings) but technical sections in mixed FR/EN.
  </behavior>
  <action>
    1. Verify `docs/` directory exists in the repo root (`ls docs/` — if not, create it).
    2. Write `docs/heuristics.md` per `<behavior>`. Use the actual deployed rule examples from `Resources/PathRules/*.json` (not the CONTEXT.md placeholders — they may have evolved during 10-01..10-04 implementation).
    3. Include a section "Comment ajouter une règle utilisateur" with a copy-pastable JSON example.
    4. Include the section "Corpus de référence" stating the concordance figure measured by Plan 10-05's acceptance test (cite the SUMMARY file by relative link: `[10-05-SUMMARY.md](../.planning/phases/10-orphan-detection-precision-refactor/10-05-SUMMARY.md)`).
    5. NO need to commit-and-test — pure documentation.
  </action>
  <verify>
    <automated>ls docs/heuristics.md &amp;&amp; wc -l docs/heuristics.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/heuristics.md` exits `0`.
    - `wc -l docs/heuristics.md` returns at least `100` (a serious doc, not a stub).
    - `grep -c "Pipeline en 7 étapes\\|7-step\\|7 étapes" docs/heuristics.md` returns at least `1`.
    - `grep -c "HardBlacklist" docs/heuristics.md` returns at least `1`.
    - `grep -c "MinRiskFloor" docs/heuristics.md` returns at least `1`.
    - `grep -c "ConfidenceScore\\|score" docs/heuristics.md` returns at least `5`.
    - `grep -c "PathRule\\|path-rules" docs/heuristics.md` returns at least `2`.
    - `grep -c "%LocalAppData%\\\\DiskScout\\\\path-rules\\|path-rules" docs/heuristics.md` returns at least `1` (user-rules folder documented).
  </acceptance_criteria>
  <done>User-facing docs shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual UAT — Score badges + Pourquoi modal + auto-launch</name>
  <files>(verification only — no files modified by this task)</files>
  <action>This is a verification checkpoint, not an implementation step. The executor pauses here so the user (or auto-approval logic) confirms that the UI built in Tasks 1-3 behaves correctly. See `<what-built>` and `<how-to-verify>` below for the verification protocol.</action>
  <verify>
    <automated>dotnet build src/DiskScout.App/DiskScout.App.csproj -c Debug --nologo &amp;&amp; dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj --nologo --verbosity minimal</automated>
  </verify>
  <done>User typed "approved" OR auto-approval criteria met (build clean + tests >= 210 + --audit CSV produced).</done>
  <what-built>
    - Score column in Rémanents tab with color-coded badges (green/lime/orange/dark-orange/red).
    - Tooltip on badge listing rule + matcher hits.
    - "Pourquoi ?" button per AppData row, opening a modal with the 7-step trace.
    - Non-AppData rows show no badge / no button (unchanged appearance).
    - docs/heuristics.md ships at the repo root.
  </what-built>
  <how-to-verify>
    1. Build clean: `dotnet build src/DiskScout.App/DiskScout.App.csproj -c Debug` exits `0`. Auto-launch (per user MEMORY.md): the executor SHOULD have already done `dotnet run --project src/DiskScout.App/DiskScout.App.csproj` at the end of Task 2. If a window is already open, skip to step 3.
    2. If no window is open, run `dotnet run --project src/DiskScout.App/DiskScout.App.csproj`. Approve the UAC prompt.
    3. Click "Scanner" with `C:\` checked. Wait for the scan to complete (~30-90 s on a typical SSD).
    4. Click the "Rémanents" tab. Expand the "AppData orphelins" group.
    5. **VERIFY**: each AppData orphan row shows a colored Score badge between the path and the size column. Color matches the RiskLevel: green=Aucun (safe), red=Critique (do not touch).
    6. **VERIFY**: hovering a badge shows a tooltip listing the triggered rules and matched sources.
    7. **VERIFY**: clicking the "Pourquoi ?" button on the same row opens a modal showing: full path, parent significant path, score X/100, risk label, category, list of triggered PathRules, list of matched signals (Service/Driver/Appx/Registry hits).
    8. **VERIFY**: non-AppData groups (StaleTemp, BrowserCache, EmptyFolder, etc.) show NO score badge and NO "Pourquoi ?" button.
    9. **VERIFY**: `docs/heuristics.md` exists and reads cleanly in a Markdown viewer.
    10. Close the app. Run `dotnet run --project src/DiskScout.App/DiskScout.App.csproj -- --audit`. Wait for it to exit (~15-30 s). Confirm the file `%LocalAppData%\DiskScout\audits\audit_*.csv` exists with header + rows.
    11. Run the FULL test suite once more: `dotnet test tests/DiskScout.Tests/DiskScout.Tests.csproj`. Expect `Passed:` >= 210, exit `0`.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 verification steps pass.
    Type "issue:badge-color-wrong" / "issue:modal-empty" / "issue:non-appdata-shows-badge" / "issue:audit-csv-missing" with details if any step fails — the executor will diagnose and fix in a follow-up commit before re-checkpointing.
    Auto-approval permitted IF AND ONLY IF: build clean (0 warnings, 0 errors) AND `dotnet test` count >= 210 AND `--audit` produces a valid CSV. Manual visual UAT is preferred but the auto-approval path is acceptable for a solo workflow.
  </resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build` exits `0`.
2. `dotnet test` exits `0` with `Passed:` >= 210.
3. App launches and the Rémanents tab shows score badges + Pourquoi modals on AppData orphans only.
4. `docs/heuristics.md` exists with >= 100 lines.
5. NO `File.Delete` / `Directory.Delete` introduced.
</verification>

<success_criteria>
- 1 modified VM (OrphansViewModel) + 1 new VM (OrphanDiagnosticsViewModel) + 1 modified XAML (OrphansTabView) + 1 new modal (OrphanDiagnosticsWindow) + 1 IValueConverter + 1 docs file + ~5 tests + 1 visual UAT.
- 4 atomic commits: `feat(10-06): extend OrphanRow with Diagnostics + add OrphanDiagnosticsViewModel + RiskLevelToBrushConverter`, `feat(10-06): Score column + tooltip + Pourquoi modal in Rémanents tab`, `docs(10-06): heuristics.md (7-step pipeline + scoring + custom rules)`, optional `chore(10-06): visual UAT approved`.
- Suite green; 210+ tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/10-orphan-detection-precision-refactor/10-06-SUMMARY.md`. Document:
- VM changes (OrphanRow + new OrphanDiagnosticsViewModel)
- XAML changes (Score column + tooltip + Pourquoi button)
- Modal layout
- docs/heuristics.md outline + line count
- UAT outcome (auto-approved or human-verified)
- Final test count
- Phase 10 closure note: cite the corpus concordance from 10-05-SUMMARY.md as the success metric
</output>
