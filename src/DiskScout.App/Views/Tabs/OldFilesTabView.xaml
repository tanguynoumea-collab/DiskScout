<UserControl x:Class="DiskScout.Views.Tabs.OldFilesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:DiskScout.Views.Controls"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <controls:EmptyStatePanel Message="{Binding EmptyStateMessage}"
                                  Visibility="{Binding HasResults, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
        <DockPanel Visibility="{Binding HasResults, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border DockPanel.Dock="Top" Padding="14,10" Background="{StaticResource BackgroundAltBrush}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Âge mini (jours) :" VerticalAlignment="Center" Foreground="{StaticResource ForegroundSubtleBrush}" Margin="0,0,8,0" />
                    <TextBox Width="80" Text="{Binding MinAgeDays, UpdateSourceTrigger=LostFocus}"
                             Background="{StaticResource BackgroundElevatedBrush}"
                             Foreground="{StaticResource ForegroundBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             Padding="6,4" VerticalAlignment="Center" />
                    <TextBlock Text="Taille mini (Mo) :" VerticalAlignment="Center" Foreground="{StaticResource ForegroundSubtleBrush}" Margin="18,0,8,0" />
                    <TextBox Width="80" Text="{Binding MinMbFilter, UpdateSourceTrigger=LostFocus, StringFormat=F1}"
                             Background="{StaticResource BackgroundElevatedBrush}"
                             Foreground="{StaticResource ForegroundBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             Padding="6,4" VerticalAlignment="Center" />
                    <TextBlock VerticalAlignment="Center" Margin="18,0,0,0" Foreground="{StaticResource ForegroundSubtleBrush}">
                        <Run Text="{Binding Count, Mode=OneWay}" /><Run Text=" fichiers — " />
                        <Run Text="{Binding TotalBytes, Converter={StaticResource BytesFormatConverter}, Mode=OneWay}" FontWeight="SemiBold" />
                    </TextBlock>
                </StackPanel>
            </Border>
            <DataGrid ItemsSource="{Binding View}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copier le chemin"
                                  Command="{Binding PlacementTarget.DataContext.CopyPathCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"
                                  CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" />
                        <Separator />
                        <MenuItem Header="Supprimer (corbeille / définitif…)"
                                  Command="{Binding PlacementTarget.DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"
                                  CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BackgroundAltBrush}" Padding="12,6" Margin="0,6,0,2">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </DataGrid.GroupStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="Âge" Binding="{Binding AgeDisplay}" Width="0.7*" SortMemberPath="AgeDays" />
                    <DataGridTextColumn Header="Modifié" Binding="{Binding LastModifiedDisplay}" Width="0.9*" SortMemberPath="LastModifiedUtc" />
                    <DataGridTextColumn Header="Taille" Binding="{Binding SizeDisplay}" Width="0.8*" SortMemberPath="SizeBytes" />
                    <DataGridTextColumn Header="Chemin" Binding="{Binding FullPath}" Width="4*" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
