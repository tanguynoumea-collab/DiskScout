<UserControl x:Class="DiskScout.Views.UninstallWizard.ResidueScanStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}"
             Foreground="{StaticResource ForegroundBrush}">
    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Bottom"
                Background="{StaticResource BackgroundAltBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Command="{Binding CancelCommand}" Width="120" Margin="0,0,8,0" />
                <Button Content="Lancer le scan" Command="{Binding ScanCommand}" Width="160" Margin="0,0,8,0" />
                <Button Content="Suivant : confirmation" Command="{Binding NextCommand}" Width="200" />
            </StackPanel>
        </Border>

        <DockPanel Margin="20">
            <Border DockPanel.Dock="Top"
                    Background="{StaticResource BackgroundAltBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                <TextBlock Text="{Binding CurrentArea}" FontWeight="SemiBold"
                           Foreground="{StaticResource ForegroundBrush}" TextWrapping="Wrap" />
            </Border>

            <Border Background="{StaticResource BackgroundAltBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="4">
                <DataGrid ItemsSource="{Binding Findings}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Background="Transparent"
                          Foreground="{StaticResource ForegroundBrush}"
                          BorderThickness="0"
                          RowBackground="Transparent"
                          AlternatingRowBackground="{StaticResource BackgroundElevatedBrush}"
                          HorizontalGridLinesBrush="{StaticResource BorderBrush}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Catégorie" Binding="{Binding Category}" Width="0.8*" />
                        <DataGridTextColumn Header="Chemin" Binding="{Binding Path}" Width="3*" />
                        <DataGridTextColumn Header="Taille" Binding="{Binding SizeBytes}" Width="0.6*" />
                        <DataGridTextColumn Header="Confiance" Binding="{Binding Trust}" Width="0.8*" />
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="0.8*" />
                        <DataGridTextColumn Header="Raison" Binding="{Binding Reason}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>
    </DockPanel>
</UserControl>
